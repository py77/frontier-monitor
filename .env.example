# Postgres connection (override for the container; default in compose works for local).
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/frontier

# Analyst version — bump (v1 -> v2) to force re-analysis after a prompt change in
# .claude/commands/refresh.md. Old signals stay; new ones get written; dashboard reads latest.
ANALYST_VERSION=v1
